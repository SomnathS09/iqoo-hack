package hack.pune.iqoo.bloomlens.model

/** Shared between native onboarding and Provider Mode's web form, so both offer the same choices. */
object PersonaOptions {
    val SKILL_LEVELS = listOf("Beginner", "Intermediate", "Advanced")
    val FOCUS_AREAS = listOf("Coding & DSA", "Math", "Science", "General")
}
